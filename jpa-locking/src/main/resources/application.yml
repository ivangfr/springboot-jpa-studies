server.port: 8082

spring:
  application:
    name: jpa-locking
  jpa:
    hibernate:
      ddl-auto: update

#logging:
#  level:
#    org.hibernate:
#      SQL: DEBUG
#      type.descriptor.sql.BasicBinder: TRACE

---
spring:
  profiles: mysql
  datasource:
    url: jdbc:mysql://localhost:3306/studiesdb?autoReconnect=true&useSSL=false&useUnicode=yes&characterEncoding=UTF-8&useLegacyDatetimeCode=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: secret

---
spring:
  profiles: postgres
  datasource:
    url: jdbc:postgresql://localhost:5432/studiesdb
    username: postgres
    password: postgres

---
spring:
  profiles: mysql-test
  datasource:
    url: jdbc:mysql://localhost:33066/studiesdb-test?autoReconnect=true&useSSL=false&useUnicode=yes&characterEncoding=UTF-8&useLegacyDatetimeCode=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&rewriteBatchedStatements=true
    username: root-test
    password: secret-test

---
spring:
  profiles: postgres-test
  datasource:
    url: jdbc:postgresql://localhost:54322/studiesdb-test
    username: postgres-test
    password: postgres-test
