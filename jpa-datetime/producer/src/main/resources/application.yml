server.port: 8084

spring:
  application:
    name: producer
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:${MYSQL_PORT:3306}/studiesdb?autoReconnect=true&useSSL=false&useUnicode=yes&characterEncoding=UTF-8&useLegacyDatetimeCode=true&serverTimezone=${app.server-timezone}&allowPublicKeyRetrieval=true
    username: root
    password: secret

app:
  server-timezone: UTC
